const SMALLER_FILE_SIZE = 400000;

export enum FILE_SIZES {
  XLSX = 10000000,
  USER_PHOTO_SIZE = SMALLER_FILE_SIZE,
  ALBUM_PHOTO_SIZE = 2000000
}

export enum QUERY_LIMIT_SIZE {
  LIMIT = 500,
  TOTAL_LIMIT = 10000
}
